<div class="navbar navbar-inverse navbar-fixed-top" role="navigation">
    <div class="container-fluid">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="~/"><i class="fa fa-cubes"></i> Guerilla Trader</a>
        </div>
        <div class="collapse navbar-collapse">
            <ul class="nav navbar-nav">
                @Html.Action("TopMenu", "Layout", new { activeMenu = ViewBag.ActiveMenu })
            </ul>
            @*<ul class="nav navbar-nav navbar-right">
                @Html.Action("LanguageSelection", "Layout")
                @Html.Action("UserMenuOrLoginLink", "Layout")
            </ul>*@
            <ul class="nav navbar-nav navbar-right">
                <li><a id="countdown" onclick="$('#setCountDownModal').modal('show')" href="javascript:void(0);">Countdown</a></li>
            </ul>
			<ul class="nav navbar-nav navbar-right">
                <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Info <span class="caret"></span></a>
                    <ul class="dropdown-menu">
                        <li><a href="#" data-toggle="modal" data-target="#todoModal">Todo</a></li>
                        <li><a href="#" data-toggle="modal" data-target="#tradingDirectivesModal">Trading Directives</a></li>
                        <li><a href="#" data-toggle="modal" data-target="#marketsModal">Markets</a></li>
                        <li><a href="#" data-toggle="modal" data-target="#consoleModal">Console</a></li>
                    </ul>
                </li>
            </ul>
            @Html.Action("TradingAccountMenu", "TradingAccounts")
        </div>
    </div>
</div>